# Добавь в pyproject.toml в секцию [project.optional-dependencies] или установи напрямую:
# pip install -r requirements-test.txt

pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27          # AsyncClient для e2e тестов
aiosqlite>=0.20      # SQLite async driver для тестовой БД
